apiVersion: v1
kind: Service
metadata:
    name: utilityadmapresentacaoservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: utilityadmapresentacaoservice